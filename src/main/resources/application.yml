spring:
  application:
    name: tdd-example-study
  datasource:
    url: jdbc:mariadb://localhost:3306/tdd_example_study
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true